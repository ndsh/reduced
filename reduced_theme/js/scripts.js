$(document).ready(function() {
    /* ----------------------------- */
    /*       DROPDOWN
    /* ----------------------------- */
    /* ----------------------------- */
    /*       SCROLLREVEAL
    /* ----------------------------- */
    window.sr = ScrollReveal();
        sr.reveal('.reveal', {
        distance: '0px',
        scale: 1,
        duration : 550,
    });
});